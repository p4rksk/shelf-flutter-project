import 'package:flutter/material.dart';

class BestSellerDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('전체 베스트셀러'),
      ),
      body: Center(
        child: Text('전체 베스트셀러 페이지입니다.'),
      ),
    );
  }
}